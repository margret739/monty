#include "monty.h"

/**
 * f_stack - prints the top
 * @head: head
 * @count: line_number
 * Return: no return
 */

void f_stack(stack_t **head, unsigned int count)
{
	(void)head;
	(void)count;
	bus.lifi = 0;
}
